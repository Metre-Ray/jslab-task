import { FilteringPipePipe } from './filtering-pipe.pipe';

describe('FilteringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilteringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
